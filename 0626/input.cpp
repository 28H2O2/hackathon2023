// C
#include<iostream>
#include<string>
#include<vector>
using namespace std;

int main() {
    string input;
    vector<string> input_line;
    while(getline(cin, input)) {
        input_line.push_back(input);
    }
    
}